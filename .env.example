VITE_API_URL=/api/v1
VITE_EUR_TO_RON=5

# Google Tag Manager container id (format GTM-XXXXXXX).
# Leave empty to fully disable analytics/marketing (no script is loaded).
# GA4, Google Ads etc. are configured inside the GTM container, no code change needed.
VITE_GTM_ID=
